node_modules/
dist/
.next/
.env
.env.*
!.env.example
*.log
coverage/
.DS_Store
Thumbs.db
# Supabase CLI local/temp artifacts only — keep the SQL source (migrations,
# data, seed, setup) tracked so deploys are reproducible.
supabase/.branches/
supabase/.temp/
supabase/.env

# Local agent/tooling dirs — never commit.
.claude/
.agent/
